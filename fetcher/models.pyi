# These are runtime-only, not real types at runtime, so stubs should not define them as type aliases.
# Remove these from the stub to match runtime and pass stubtest.

# These are runtime variables, not types or attributes, but stubtest expects them to exist as variables.
Status: object
FileType: object
